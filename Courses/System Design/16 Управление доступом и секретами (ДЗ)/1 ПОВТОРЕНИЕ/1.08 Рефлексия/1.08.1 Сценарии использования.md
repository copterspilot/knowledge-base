Тема криптографии и защиты данных — фундаментальная часть информационной безопасности, где взаимодействие между сервисами, хранение конфиденциальных данных и защита трафика имеют критическое значение.

Ниже приведён **структурированный обзор ключевых криптографических технологий**, включая **обоснованные сценарии их использования**, **назначение** и **угрозы, которые они устраняют**. Также добавлены **дополнительные важные технологии** для полноты картины.

---

## 🔐 1. Симметричное шифрование

### 📌 Описание
Использует **один и тот же ключ** для шифрования и расшифровки данных. Примеры: AES, DES, 3DES, ChaCha20.

### ✅ Сценарий использования
**Шифрование конфиденциальных данных в хранилище (например, секретов в базе или файле конфигурации)**

- Например: хранение паролей, API-ключей, токенов в зашифрованном виде в etcd (Kubernetes Secrets) или в Vault.

### 🎯 Назначение
Обеспечение **конфиденциальности данных** при хранении (data at rest).

### ⚠️ Устраняемые угрозы
- Неавторизованный доступ к данным при компрометации хранилища (например, утечка диска или базы).
- Чтение конфиденциальной информации злоумышленником.

> 🔍 **Пример:** Kubernetes использует AES для шифрования Secrets при включённой функции **EncryptionConfiguration**.

---

## 🔐 2. Асимметричное шифрование (с открытым ключом)

### 📌 Описание
Использует пару ключей: **открытый (public)** и **закрытый (private)**. Шифрование — открытым, расшифровка — закрытым. Примеры: RSA, ECC.

### ✅ Сценарий использования
**Безопасный обмен симметричным ключом (например, в TLS handshake)**

- Например: клиент и сервер договариваются о сессионном ключе с помощью RSA или ECDH.

### 🎯 Назначение
Решение проблемы **безопасной передачи ключей** без предварительного общего секрета.

### ⚠️ Устраняемые угрозы
- Перехват ключа при передаче по незащищённому каналу.
- Подмена ключа (в комбинации с PKI и сертификатами).

> 🔍 **Пример:** В TLS клиент шифрует "предмастер-секрет" открытым ключом сервера. Только сервер может расшифровать его своим приватным ключом.

---

## 🔗 3. Хэширование

### 📌 Описание
Функция, преобразующая данные в **фиксированную по длине "отпечаток" (хэш)**. Примеры: SHA-256, SHA-3, MD5 (устарел).

### ✅ Сценарий использования
**Проверка целостности данных (например, образов контейнеров или конфигураций)**

- Например: Docker использует хэши (SHA256) для идентификации слоёв образа. CI/CD пайплайн проверяет, не был ли образ изменён.

### 🎯 Назначение
Обеспечение **целостности данных** — проверка, что данные не были изменены.

### ⚠️ Устраняемые угрозы
- Несанкционированное изменение данных (подмена образа, конфига).
- Замена файла без ведома системы.

> 🔍 **Пример:** Helm чарты могут подписываться, а их хэши — проверяться для подтверждения подлинности.

---

## 🔒 4. TLS (Transport Layer Security)

### 📌 Описание
Протокол шифрования трафика на транспортном уровне. Обеспечивает **конфиденциальность, целостность и аутентификацию сервера**.

### ✅ Сценарий использования
**Шифрование трафика между клиентом и веб-сервисом (например, ingress-контроллером)**

- Например: пользователь подключается к веб-приложению через HTTPS, защищённое TLS.

### 🎯 Назначение
- Защита трафика от прослушивания (eavesdropping).
- Аутентификация сервера (по сертификату).
- Предотвращение подмены данных в пути.

### ⚠️ Устраняемые угрозы
- Перехват трафика (man-in-the-middle).
- Подмена сервера (если сертификат валиден).
- Модификация данных в транзите.

> 🔍 **Пример:** Ingress-контроллер (NGINX, Traefik) использует TLS для шифрования HTTPS-трафика.

---

## 🔐🔁 5. Взаимный TLS (mTLS, mutual TLS)

### 📌 Описание
Расширение TLS, при котором **аутентифицируются и клиент, и сервер** с помощью сертификатов.

### ✅ Сценарий использования
**Сервис-сервисная аутентификация в mesh-сети (например, Istio)**

- Например: микросервис `orders` может общаться с `payments` только если оба предоставят валидные сертификаты.

### 🎯 Назначение
Обеспечение **двусторонней аутентификации** и **доверия между сервисами**.

### ⚠️ Устраняемые угрозы
- Подмена сервиса (например, подключение "злого" пода к бэкенду).
- Неавторизованный доступ к внутренним API.
- Lateral movement атакующего внутри кластера.

> 🔍 **Пример:** Istio автоматически настраивает mTLS между sidecar-прокси (Envoy), обеспечивая "zero-trust" сеть.

---

## 🧩 Дополнительные технологии (для полноты картины)

### 6. **PKI (Public Key Infrastructure)**

- **Описание:** Система управления цифровыми сертификатами и центрами сертификации (CA).
- **Сценарий:** Выпуск и отзыв сертификатов для сервисов в кластере.
- **Назначение:** Обеспечение доверия к сертификатам.
- **Угрозы:** Поддельные сертификаты, компрометация CA.

> 🔍 **Пример:** Istio использует встроенный CA или внешний (например, Vault) для выпуска сертификатов.

---

### 7. **Сертификаты и их управление (Let’s Encrypt, cert-manager)**

- **Описание:** Автоматическое получение и обновление TLS-сертификатов.
- **Сценарий:** Автоматическое обновление HTTPS-сертификатов для Ingress.
- **Назначение:** Предотвращение простоев из-за просроченных сертификатов.
- **Угрозы:** Компрометация сервиса из-за просроченного сертификата.

> 🔍 **Пример:** cert-manager + Let’s Encrypt в Kubernetes.

---

### 8. **Подпись артефактов (Sigstore, Cosign)**

- **Описание:** Подпись образов контейнеров цифровой подписью.
- **Сценарий:** Проверка, что образ был собран доверённым CI/CD-пайплайном.
- **Назначение:** Обеспечение подлинности и целостности образов.
- **Угрозы:** Использование поддельного или скомпрометированного образа.

> 🔍 **Пример:** Cosign + Fulcio + Rekor (Sigstore) для шифрования и аудита образов.

---

### 9. **Шифрование на уровне диска (Disk Encryption)**

- **Описание:** Шифрование данных на физическом носителе (например, LUKS, dm-crypt, AWS EBS encryption).
- **Сценарий:** Защита данных при утере/уклонении диска.
- **Назначение:** Защита **data at rest** на уровне ОС/облака.
- **Угрозы:** Физический доступ к диску, кража сервера.

---

### 10. **OAuth2 / OpenID Connect (OIDC)**

- **Описание:** Протоколы аутентификации и авторизации.
- **Сценарий:** Вход пользователя в Kubernetes (через `kubectl login`) или в веб-интерфейс.
- **Назначение:** Централизованная аутентификация.
- **Угрозы:** Подбор паролей, использование учётных данных без MFA.

> 🔍 **Пример:** Аутентификация в Kubernetes через Google Workspace или Keycloak.

---

## 📊 Сводная таблица

| Технология | Сценарий | Назначение | Устраняемые угрозы |
|-----------|--------|-----------|-------------------|
| **Симметричное шифрование** | Шифрование секретов в etcd | Конфиденциальность данных | Утечка данных при доступе к хранилищу |
| **Асимметричное шифрование** | Обмен ключами в TLS | Безопасная передача ключей | Перехват ключа |
| **Хэширование** | Проверка образа контейнера | Целостность данных | Подмена образа |
| **TLS** | HTTPS-трафик к веб-сервису | Шифрование и аутентификация сервера | MITM, прослушивание |
| **mTLS** | Взаимодействие микросервисов в Istio | Двусторонняя аутентификация | Подмена сервиса, lateral movement |
| **PKI** | Управление сертификатами | Доверие к сертификатам | Поддельные сертификаты |
| **cert-manager** | Автообновление сертификатов | Непрерывная безопасность | Просроченные сертификаты |
| **Подпись образов** | Проверка образа перед запуском | Подлинность артефактов | Запуск скомпрометированного образа |
| **Шифрование диска** | Защита данных на носителе | Защита data at rest | Физический доступ к диску |
| **OAuth2/OIDC** | Вход в систему | Централизованная аутентификация | Неавторизованный доступ |

---

## 💡 Рекомендации для занятия

- Свяжите технологии с реальными DevOps-практиками:  
  → mTLS в Istio,  
  → шифрование Secrets,  
  → cert-manager в Ingress,  
  → подпись образов в GitLab CI.

- Покажите **цепочку доверия**: от хэша → к подписи → к сертификату → к CA.

- Используйте **аналогии**:
  - Хэш — как отпечаток пальца.
  - mTLS — как пропуск с фотографией и проверкой с обеих сторон.

---

Если хотите, могу:
- подготовить **слайды** по этой теме,
- создать **интерактивный кейс** (например, "нарушитель внедрил под — как mTLS это предотвратил?"),
- или разработать **тест/опрос** для студентов.

Готов помочь!